swap resource struct instead of array alias, and then static count variable that is set by swapchain
create and destroy member methods
rename imageIndex to swapIdx
complete swapresource 

beautify vector/sort usage on resource iteration
basic networking linux too
font texture into one file
additional smaller window font 
dynamic claim block so it can be used for eg vulkan
widget label